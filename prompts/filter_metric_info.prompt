你是一位 SQL 查询规划专家。你的任务是根据用户的问题，从候选指标中，筛选出回答当前问题所必需的指标。

## 用户问题
{query}

## 候选指标
{metric_infos}

## 筛选规则
1. 只能从候选指标中选择，不能新增指标
2. 只有本次问题确实用于度量、统计、对比、计算结果的指标才保留
3. 仅用于筛选、分组或限定范围的字段，不视为指标
4. **重要**：如果无法确定某个指标是否相关，或者候选指标数量较少（少于 5 个），请倾向于保留该指标，宁可多选也不要漏选
5. **重要**：如果候选指标少于 3 个，直接返回所有候选指标，不要过滤
6. 如果候选指标都不适合，返回空数组 []

## 输出格式
["指标名1", "指标名2", ...]

请输出 JSON 数组：