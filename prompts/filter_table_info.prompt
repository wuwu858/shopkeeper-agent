你是一位 SQL 查询规划专家。你的任务是根据用户的问题，从候选表和字段中，筛选出回答当前问题所必需的表和字段。

## 用户问题
{query}

## 候选表结构
{table_infos}

## 筛选规则
1. 只能从候选表和候选字段中选择，不能新增表或字段
2. 每张保留下来的表，至少要有一个字段被选中
3. 字段是否保留，以"本次查询是否实际使用"为标准
4. 如果选择多张表，要保留 JOIN 所需的主外键字段
5. 只输出 JSON 对象，不输出解释文字

## 输出格式
{{
    "表名1": ["字段1", "字段2", "..."],
    "表名2": ["字段1", "字段2", "..."]
}}

请输出 JSON 对象：